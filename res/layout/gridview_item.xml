<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center" >

    <ImageView
        android:layout_width="@dimen/gridview_item_imageview_width"
        android:layout_height="@dimen/gridview_item_imageview_height"
        android:id="@+id/gridview_item_imageview"
        android:src="@drawable/ic_launcher"
        android:contentDescription="@string/game_contentDescription"
      
         />
    <TextView 
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/gridview_item_tv_text"
        android:id="@+id/gridview_item_textview"
        android:textSize="@dimen/gridview_item_tv_size"
        android:gravity="center_horizontal"/>

</LinearLayout>